# Ship24 fixtures

- `delivered.json` — constructed after the shape of one signed
  `/api/parcels/{number}` reply (verified 2026-09-10): a delivered shipment with
  two reported couriers, one leg without an offset, and recipient, signature and
  access-code values that the projection must drop. Identifiers are made up.
