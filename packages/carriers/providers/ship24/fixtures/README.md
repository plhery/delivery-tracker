# Ship24 fixtures

- `delivered.json`: a synthetic `/api/parcels/{number}` reply for a delivered shipment,
  with two reported couriers, one leg without an offset, and recipient, signature and
  access-code values the parser must drop. Identifiers are made up.
