# ParcelsApp fixtures

- `announced.json` — constructed after the shape of one captured
  `/api/v2/parcels` reply (verified 2026-09-08): an announced-only shipment
  whose newest row is a delivery-preference notice rather than a scan, plus
  sender and destination values that the projection must drop. Identifiers are
  made up.
