# 17TRACK fixtures

- `delivered.json`: a synthetic `track/restapi` reply for a delivered shipment, with a
  declared stage per event, one reported carrier name, and recipient address, phone and
  signature values the parser must drop. Identifiers are made up.
