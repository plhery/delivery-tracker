# 17TRACK fixtures

- `delivered.json` — constructed after the shape of one captured `track/restapi`
  reply (verified 2026-09-10): a delivered shipment with a declared provider
  stage per event, one reported carrier name, and recipient address, phone and
  signature values that the projection must drop. Identifiers are made up.
