# Evri fixtures

- `international.html`: GlobalEco detail card and history table, with a duplicate scan and the partner alias in the `tracking_number` input (the heading and System Tracking field carry the Evri number).
- `not-found.html`: the explicit empty result with its cleared input.

Both are synthetic HTML in the portal's structure; no live parcel.
