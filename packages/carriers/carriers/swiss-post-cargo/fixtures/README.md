# Swiss Post Cargo fixtures

| File | Scenario | Provenance |
| --- | --- | --- |
| `delivered.json` | Type 1 response for a delivered shipment: two history rows (`DLV`, `RFS`), plus the consignee and full-description fields the projection must drop. | Constructed in the shape of the public endpoint; the identifier and every name are made up. |
