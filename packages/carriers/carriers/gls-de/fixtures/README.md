`parcel-delivered.json`: one delivered GROUP parcel row with a German scan
location, the `REQUEST` owner code, and recipient name and street the parser
must drop. Tests use it as both the overview row and the detail body. Constructed
in the endpoint shape; the parcel number is synthetic.
