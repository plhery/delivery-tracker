# PostNL fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | `tracking-items` answer for a delivered international parcel: four events from pre-advice to delivery, each with its category and country, a webshop sender name, and the recipient and signature fields such an item can carry. | Constructed after the documented response shape. `LX123456785NL` is the checksum-valid synthetic number already in `numbers.json`; the shop, the names, the address and the signature URL are made up. |
