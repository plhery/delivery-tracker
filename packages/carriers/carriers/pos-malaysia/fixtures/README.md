# Fixtures

`delivered.json` is one delivered `data[]` entry with the six `process_summary` values from collection to delivery, plus sender and recipient blocks and the `epod` proof-of-delivery slot the adapter must drop. Modelled on the demo parcel in the tracking app's bundle; identifiers, postcodes, phone numbers and times are made up.
