# Pos Malaysia fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | One `data[]` entry for a delivered consignment: `process_status` "DELIVERED" and the six `process_summary` values from collection to delivery, plus the sender and recipient blocks and the `epod` proof-of-delivery slot that must never be retained. | Constructed after the vendor's own shipped demo parcel in the tracking SPA bundle (inspected 2026-09-10). Every identifier, postcode, phone number and timestamp is made up. |
