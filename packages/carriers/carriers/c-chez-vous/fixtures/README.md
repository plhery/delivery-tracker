# C Chez Vous fixtures

- `out-for-delivery.json` — the record embedded in `<tracking :tracking-results="…">` for a
  single-parcel order on step 4, built to the page's shape. Every personal value is a synthetic
  placeholder that the privacy assertion looks for.
