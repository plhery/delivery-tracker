# Hermes Einrichtungs-Service fixtures

- `delivered.json` — scrubbed delivered order with both streams (`statusdaten` is ignored) and
  a `null`-wording row; the number is the carrier's published sample.
- `empty-order.json` — scrubbed all-`null` placeholder returned for an unknown number.
