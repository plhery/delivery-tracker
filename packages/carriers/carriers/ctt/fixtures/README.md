# CTT fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | `ObjectEventsFromQuery` record for a delivered parcel: accepted then delivered, both `StateId`s mapped, with the sender/recipient identity block the record carries. | Constructed after the live 2026-09-10 response shape. Every identifier, name, address and timestamp is made up; `RL000000005PT` is a checksum-valid synthetic S10 PT number. |
