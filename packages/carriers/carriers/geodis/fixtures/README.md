# GEODIS fixtures

- `out-for-delivery.json` — the `contenu` block of a shipment out for delivery: an active
  timeline step, two days of scans with agency names, a planned date, and the sender,
  recipient, complementary-information and document blocks. Built to the endpoint's shape with
  a synthetic shipment number; every personal value is a placeholder the privacy test looks for.
