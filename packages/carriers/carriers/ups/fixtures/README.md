# UPS fixtures

`out-for-delivery.json`: a `GetStatus` reply for a parcel out for delivery, with UTC and local/offset scan times, a scheduled day, and the recipient, address, signature and photo fields the privacy test asserts are dropped. Every value is made up; `1Z999AA10123456784` is synthetic and the `PRIVATE …` strings are placeholders.
