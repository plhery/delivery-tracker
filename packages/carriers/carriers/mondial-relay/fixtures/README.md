# Mondial Relay fixtures

- `available-at-relay.json`: `/api/tracking` reply for a parcel waiting at its Point Relais (headline, two events, two milestones, an estimate, relay and recipient blocks). All values are made up; the `PRIVATE …` placeholders are what the privacy assertion looks for.
