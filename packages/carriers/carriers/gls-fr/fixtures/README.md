`delivered.json`: a delivered parcel with two scans (`CON`, then `LIV`) and a
planned delivery date. Constructed in the consignee endpoint shape with synthetic
numbers; every recipient, address, contact, signature and instruction field is a
placeholder the privacy test looks for in the result.
