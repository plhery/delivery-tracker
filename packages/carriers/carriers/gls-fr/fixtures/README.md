# GLS France fixtures

- `delivered.json` — a delivered parcel with two scans (CON, then LIV) and a
  planned delivery date. Constructed to the shape of the public consignee
  endpoint; the identifiers are the synthetic numbers from `numbers.json` and
  every recipient, address, contact, signature and instruction field is a
  placeholder that the privacy assertion looks for in the result.
