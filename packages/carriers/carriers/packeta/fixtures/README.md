# Packeta fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | The `item` object of a delivered parcel: `packetStatusId` "3", the merchant name and Z-BOX pickup point, four canned English event sentences spanning registration to collection, plus the recipient identity and signature fields that must never be retained. | Constructed after the live 2026-09-10 contract and the prior-art payload samples. Every identifier, name, address and timestamp is made up. |
