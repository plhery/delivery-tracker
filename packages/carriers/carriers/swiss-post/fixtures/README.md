`out-for-delivery.json`: a domestic parcel out for delivery (summary with a
delivery window, three events, matching translation patterns) plus recipient,
address, signature and contact fields the parser must drop. Constructed in the
public result shape; the number and names are synthetic.
