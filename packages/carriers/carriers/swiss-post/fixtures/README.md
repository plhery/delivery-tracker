# Swiss Post fixtures

| File | Scenario | Provenance |
| --- | --- | --- |
| `out-for-delivery.json` | A domestic parcel out for delivery: the shipment summary with a calculated date and a delivery window, three dated events with scan locations, the matching translation patterns, plus the recipient, address, signature and contact fields the projection must drop. | Constructed in the shape of the public `service.post.ch` search result; the shipment number is the folder's synthetic sample and every name is made up. |
