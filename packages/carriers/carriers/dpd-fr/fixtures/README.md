# DPD France fixtures

| File | Scenario | Provenance |
|---|---|---|
| `claim-after-delivery.json` | Trace-page rows and details blocks for an outbound parcel delivered on 23 January and then subject to a claim, next to the return leg the same page describes, plus the private page sections the projection must drop. The test renders these rows into provider-shaped HTML. | Constructed after the trace page's markup. Every identifier, name, address and reference is made up; `250123456789012` and `10612345678901` are synthetic numbers that were never issued. |
