# DPD France fixtures

- `claim-after-delivery.json`: trace-page rows and detail blocks for an outbound parcel delivered then claimed, next to its return leg, plus the private sections the parser must skip. The test renders them into provider-shaped HTML. All identifiers, names, addresses and numbers are synthetic.
