# Royal Mail fixtures

- `delivered.json`: microsummary reply with a delivered summary and four scans; the recipient prose is dropped.
- `in-transit.json`: an in-transit summary, an estimate and two scans.

Both are reconstructed from the public application bundle, not recorded replies; identifiers, places, dates and the recipient are synthetic.
