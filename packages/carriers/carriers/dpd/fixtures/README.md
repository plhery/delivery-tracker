# DPD Switzerland fixtures

- `ready-for-collection.json`: guest-API `parcels/details` payload for a parcel waiting at a parcelshop, with a delivery window, a webshop sender and the recipient block the payload carries. Constructed; every identifier, name, address and time is synthetic.
