# La Poste fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | `suivi-unifie` response for a delivered Colissimo parcel: two events with their `group`/`code` pairs, a completed timeline step, a delivery estimate, and the recipient address block the feed carries on both the shipment and its events. | Constructed after the unified feed's response shape. Every identifier, name, address and timestamp is made up; `AB12345678901` is a synthetic 13-character number that was never issued. |
