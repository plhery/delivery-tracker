# Correos fixtures

| File | Scenario | Provenance |
|---|---|---|
| `delivered.json` | `codError` "0" localizador envelope for a delivered parcel: admitted, classified, out for delivery, delivered, plus the office, weight, dimension and customer blocks the envelope carries and the address, phone and signature fields that must never be retained. | Constructed after the live 2026-09-10 envelope shape and the prior-art payload samples. Every identifier, name, address and timestamp is made up. |
