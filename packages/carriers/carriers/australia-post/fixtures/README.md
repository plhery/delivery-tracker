# Fixtures

- `delivered.json`: a delivered shipments-gateway reply with twelve events, separate article and detail identity, explicit offsets, epoch milliseconds, and a summary modification time that is not a scan time.
- `not-found.json`: the matched entry's `400` / `errorCode: 21` signature inside HTTP 200.

Identifiers, facilities, names, addresses and dates are synthetic.
