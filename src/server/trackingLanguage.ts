// The wording classifier lives in the carrier package; this module keeps the
// historical import path for host code and adapters not yet moved.
export { trackingLanguageStage, languageStageStatus } from '@carriers/core/status';
