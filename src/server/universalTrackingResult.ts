// The shared universal-provider projection lives in the carrier package; this
// module keeps the historical import path for host code and grouped tests.
export * from '@carriers/providers/shared/result';
