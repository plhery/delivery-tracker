// Moved to the carrier package; this path stays for host code and grouped tests.
// `GLSGermanyTracker.recognizes` is reached through this path by the carrier
// detection route.
export * from '@carriers/carriers/gls-de/adapter';
