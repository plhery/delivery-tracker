// Moved to the carrier package; this path stays for host code and grouped tests.
// `validateDachserTrackingUrl` is reached through this path by ./carriers.
export * from '@carriers/carriers/dachser/adapter';
