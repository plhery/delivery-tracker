// The local Chromium session lives in the carrier package; this module keeps
// the historical import path for providers not yet moved.
export { scrapeUniversalPage } from '@carriers/core/transport/browser';
export type { UniversalBrowserOptions } from '@carriers/core/transport/browser';
