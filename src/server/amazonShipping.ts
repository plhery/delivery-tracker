// Moved to the carrier package; this path stays for host code and grouped tests.
// `./amazonShippingEligibility` stays in the host and imports through here.
export * from '@carriers/carriers/amazon-shipping/adapter';
